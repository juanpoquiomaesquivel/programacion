/*#include <stdio.h>
#include <math.h>

int main()
{
    double x = 1.7;
    double op;

    printf("# Ingrese el valor de \'x\': ");
    scanf("%lf", &x);

    op = sqrt(1 + pow(exp(x) / pow(x, 2), 2));

    printf("El resultado de la operacion es: %.3lf\n", op);

    return 0;
}*/