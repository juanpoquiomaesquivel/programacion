/*#include <stdio.h>
#include <math.h>

int main()
{
    double a = 1, b = -8, c = 15;
    double x1, x2;

    printf("# Ingrese el valor de \'a\': ");
    scanf("%lf", &a);
    printf("# Ingrese el valor de \'b\': ");
    scanf("%lf", &b);
    printf("# Ingrese el valor de \'c\': ");
    scanf("%lf", &c);

    x1 = (-b + sqrt(pow(b, 2) - 4 * a * c)) / (2 * a);
    x2 = (-b - sqrt(pow(b, 2) - 4 * a * c)) / (2 * a);

    printf("Las soluciones son: %.3lf y %.3lf\n", x1, x2);

    return 0;
}*/